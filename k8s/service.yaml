apiVersion: v1
kind: Service
metadata:
  name: launchpad-mcp-service
  namespace: launchpad
  labels:
    app: launchpad-mcp
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: launchpad-mcp